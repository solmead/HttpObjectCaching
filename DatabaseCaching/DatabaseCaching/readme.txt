
	 
      The DatabaseCaching library has been renamed to Utilities.Caching.Database.
      This package is empty, but will install the new packages.

	  The new package is targeting .net standard 2.0

	  An entry in the app start will need to be added to call Utilities.Caching.Database.Startup.InitDatabaseCache() with the connection string to use.